package com.ManuLifeApplication.backend;

//import net.sf.jasperreports.engine.JasperPrint;
import java.io.InputStream;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class ReportService {

//    public JasperPrint generateUserReport() throws Exception {
//        // Load and compile the Jasper template
//        InputStream inputStream = getClass().getResourceAsStream("/user_report.jrxml");
//        JasperReport jasperReport = JasperCompileManager.compileReport(inputStream);
//
//        // Prepare parameters (if any)
//        Map<String, Object> parameters = new HashMap<>();
//        // Add data source (e.g., JPA, JDBC, or a collection)
//        JRDataSource dataSource = new JRBeanCollectionDataSource(fetchUserData());
//
//        // Fill the report
//        return JasperFillManager.fillReport(jasperReport, parameters, dataSource);
//    }
//
//    private List<User> fetchUserData() {
//        // Fetch users from the database
//        // return repository.findAll();
//        return Arrays.asList(new User("John", "Doe"), new User("Jane", "Smith"));
//    }



}
